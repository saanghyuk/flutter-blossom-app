import { AttributionParameter } from "../attribution";
import { CTAParameter } from "../cta";
interface DeeplinkOption {
    deeplinks?: {
        ios?: string;
        android?: string;
        desktop?: string;
    };
    fallbacks?: {
        ios?: 'itunes-appstore' | 'google-play' | string;
        android?: 'google-play' | 'itunes-appstore' | string;
    };
    redirect?: boolean;
    desktopPopUp?: boolean;
    defaultParams?: AttributionParameter;
    ctaParams?: CTAParameter;
}
export { DeeplinkOption };
export { AttributionParameter };
